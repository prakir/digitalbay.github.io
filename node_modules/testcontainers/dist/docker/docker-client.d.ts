import Dockerode from "dockerode";
import { Host } from "./types";
import "../testcontainers-properties-file";
declare type DockerClient = {
    host: Host;
    dockerode: Dockerode;
};
export declare const dockerClient: () => Promise<DockerClient>;
export {};
