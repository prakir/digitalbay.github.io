import { DockerImageName } from "../../../docker-image-name";
import Dockerode from "dockerode";
export declare const imageExists: (dockerode: Dockerode, imageName: DockerImageName) => Promise<boolean>;
