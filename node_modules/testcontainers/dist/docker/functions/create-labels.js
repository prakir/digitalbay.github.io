"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createLabels = void 0;
const session_id_1 = require("../session-id");
const labels_1 = require("../../labels");
const createLabels = (reusable, dockerImageName, extraLabels = {}) => {
    const labels = Object.assign({}, extraLabels);
    if (dockerImageName && dockerImageName.isReaper()) {
        return labels;
    }
    if (!reusable) {
        labels[labels_1.LABEL_SESSION_ID] = session_id_1.sessionId;
    }
    return labels;
};
exports.createLabels = createLabels;
