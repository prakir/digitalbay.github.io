import { DockerImageName } from "../../docker-image-name";
import { Labels } from "../types";
export declare const createLabels: (reusable: boolean, dockerImageName?: DockerImageName | undefined, extraLabels?: Labels) => Labels;
