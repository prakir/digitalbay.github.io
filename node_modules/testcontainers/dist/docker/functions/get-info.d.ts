import Dockerode from "dockerode";
declare type DockerInfo = {
    serverVersion: number;
    operatingSystem: string;
    operatingSystemType: string;
    architecture: string;
    cpus: number;
    memory: number;
};
export declare const getDockerInfo: (dockerode: Dockerode) => Promise<DockerInfo>;
export {};
