import Dockerode from "dockerode";
export declare type StopContainerOptions = {
    timeout: number;
};
export declare const stopContainer: (container: Dockerode.Container, options: StopContainerOptions) => Promise<void>;
