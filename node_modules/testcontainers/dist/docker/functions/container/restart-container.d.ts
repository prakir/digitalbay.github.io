import Dockerode from "dockerode";
export declare type RestartContainerOptions = {
    timeout: number;
};
export declare const restartContainer: (container: Dockerode.Container, options: RestartContainerOptions) => Promise<void>;
