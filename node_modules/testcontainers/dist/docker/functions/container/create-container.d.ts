import { DockerImageName } from "../../../docker-image-name";
import Dockerode from "dockerode";
import { PortWithOptionalBinding } from "../../../port";
import { BindMount, Command, ContainerName, Env, ExtraHost, HealthCheck, Labels, NetworkMode, TmpFs, Ulimits } from "../../types";
export declare type CreateContainerOptions = {
    imageName: DockerImageName;
    env: Env;
    cmd: Command[];
    entrypoint?: string[];
    bindMounts: BindMount[];
    tmpFs: TmpFs;
    exposedPorts: PortWithOptionalBinding[];
    name?: ContainerName;
    reusable: boolean;
    labels?: Labels;
    networkMode?: NetworkMode;
    healthCheck?: HealthCheck;
    useDefaultLogDriver: boolean;
    privilegedMode: boolean;
    autoRemove: boolean;
    extraHosts: ExtraHost[];
    ipcMode?: string;
    ulimits?: Ulimits;
    addedCapabilities?: string[];
    droppedCapabilities?: string[];
    user?: string;
};
export declare const createContainer: (options: CreateContainerOptions) => Promise<Dockerode.Container>;
