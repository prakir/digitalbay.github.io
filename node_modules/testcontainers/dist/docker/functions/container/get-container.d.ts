import Dockerode from "dockerode";
import { Id } from "../../types";
export declare const getContainerById: (id: Id) => Promise<Dockerode.Container>;
export declare const getContainerByHash: (hash: string) => Promise<Dockerode.Container | undefined>;
