import { Command, ExecResult } from "../../types";
import Dockerode from "dockerode";
declare type ExecContainerOptions = {
    tty: boolean;
    stdin: boolean;
    detach: boolean;
};
export declare const execContainer: (container: Dockerode.Container, command: Command[], options: ExecContainerOptions, shouldLog?: boolean) => Promise<ExecResult>;
export {};
