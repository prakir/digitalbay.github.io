import Dockerode from "dockerode";
export declare type RemoveContainerOptions = {
    removeVolumes: boolean;
};
export declare const removeContainer: (container: Dockerode.Container, options: RemoveContainerOptions) => Promise<void>;
