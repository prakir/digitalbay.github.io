"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = __importDefault(require("path"));
const logger_1 = require("./logger");
const os_1 = require("os");
const fs_1 = require("fs");
const properties_reader_1 = __importDefault(require("properties-reader"));
const file = path_1.default.resolve((0, os_1.homedir)(), ".testcontainers.properties");
if ((0, fs_1.existsSync)(file)) {
    logger_1.log.debug("Found .testcontainers.properties file");
    const string = (0, fs_1.readFileSync)(file, { encoding: "utf-8" });
    const properties = (0, properties_reader_1.default)("").read(string);
    const host = properties.get("docker.host");
    if (host !== null) {
        logger_1.log.debug(`Setting DOCKER_HOST to ${host}`);
        process.env.DOCKER_HOST = host;
    }
    const tlsVerify = properties.get("docker.tls.verify");
    if (tlsVerify !== null) {
        logger_1.log.debug(`Setting DOCKER_TLS_VERIFY to ${tlsVerify}`);
        process.env.DOCKER_TLS_VERIFY = `${tlsVerify}`;
    }
    const certPath = properties.get("docker.cert.path");
    if (certPath !== null) {
        logger_1.log.debug(`Setting DOCKER_CERT_PATH to ${certPath}`);
        process.env.DOCKER_CERT_PATH = certPath;
    }
}
