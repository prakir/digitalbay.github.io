import { GenericContainer } from "../../generic-container/generic-container";
import { AbstractStartedContainer } from "../abstract-started-container";
import { StartedTestContainer } from "../../test-container";
export declare class MongoDBContainer extends GenericContainer {
    private readonly dockerImageName;
    constructor(image?: string);
    start(): Promise<StartedMongoDBContainer>;
    protected postStart(startedTestContainer: StartedTestContainer): Promise<void>;
    private initReplicaSet;
    private executeMongoEvalCommand;
    private buildMongoEvalCommand;
    private getMongoCmdBasedOnImageTag;
    private checkMongoNodeExitCode;
    private buildMongoWaitCommand;
}
export declare class StartedMongoDBContainer extends AbstractStartedContainer {
    constructor(startedTestContainer: StartedTestContainer);
    getConnectionString(): string;
}
