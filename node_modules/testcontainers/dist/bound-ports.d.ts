import { Port, PortWithOptionalBinding } from "./port";
import { InspectResult } from "./docker/functions/container/inspect-container";
export declare class BoundPorts {
    private readonly ports;
    getBinding(port: Port): Port;
    setBinding(key: Port, value: Port): void;
    iterator(): Iterable<[Port, Port]>;
    filter(ports: PortWithOptionalBinding[]): BoundPorts;
    static fromInspectResult(inspectResult: InspectResult): BoundPorts;
}
