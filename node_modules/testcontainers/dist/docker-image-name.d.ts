export declare class DockerImageName {
    readonly registry: string | undefined;
    readonly image: string;
    readonly tag: string;
    private readonly string;
    private static readonly HELPER_CONTAINERS;
    constructor(registry: string | undefined, image: string, tag: string);
    equals(other: DockerImageName): boolean;
    toString(): string;
    isReaper(): boolean;
    isHelperContainer(): boolean;
    static fromString(string: string): DockerImageName;
    private static getRegistry;
    private static isRegistry;
}
